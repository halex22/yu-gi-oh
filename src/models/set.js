export default class Set{

  constructor(name, code, rarity, rarityCode, price) {
    this.name = name
    this.code = code 
    this.rarity = rarity
    this.rarityCode = rarityCode
    this.price = price
  }

}